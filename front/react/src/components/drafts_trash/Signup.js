import React from 'react'

function Signup() {
    return(
        <button
        className="paybutton"
        onClick={console.log("sign")}>
        SIGN UP
        </button>
    )
}

export default Signup